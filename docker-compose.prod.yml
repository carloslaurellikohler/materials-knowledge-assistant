services:
  qdrant:
    restart: unless-stopped

  redis:
    restart: unless-stopped

  backend:
    restart: unless-stopped

  worker:
    restart: unless-stopped

  frontend:
    restart: unless-stopped
